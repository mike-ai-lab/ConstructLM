import React from 'react';

// This component is now deprecated as drawing controls have been moved to the main header
// Keeping it as a placeholder to avoid breaking imports
const DrawingToolbar: React.FC = () => {
  return null;
};

export default DrawingToolbar;